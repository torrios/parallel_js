# Simple webserver to test concurrent AJAX requests

## To install

* install Golang
* clone repo
* execute ```go run main.go``` in a terminal from a root directory
* navigate to http://localhost:8080
* Voila


